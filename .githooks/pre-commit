#!/usr/bin/env bash
set -euo pipefail

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks nije instaliran — commit prekinut." >&2
  exit 1
fi

gitleaks git --pre-commit --staged --redact --no-banner
