node_modules/
.next/
dist/
*.tsbuildinfo
.env
.env.local
.env*.local
data/scanovi-arhiva/
*.log
.DS_Store

# lokalno stanje Supabase CLI-ja (project-ref, pooler-url) — nikad u repo
supabase/.temp/
supabase/.branches/

# lokalna podešavanja Claude Code-a (dozvole po mašini) — nisu deo projekta
.claude/settings.local.json

# stanje Google budzeta (api-budget.ts) — nikad u repo
.cache/
# izlaz CLI scanova i harvesta
out/

# Clerk keyless (apps/web/.clerk/.tmp/keyless.json) — nikad u repo (Faza 7, 7.5)
apps/web/.clerk/

# lokalni pnpm store (nastao u sesiji 2026-08; nikad u repo)
.pnpm-store/
